# 1.3.1 更新日志

这是一个更新内容测试

- 新增更新通知功能  
  应用现在可以检查新版本，并在发现更新时展示更新说明与下载入口。

- 由 multiChat 创建的微信实例增加特殊标识  
  如果某个多开微信是由 multiChat 制作的，会在名称前显示一个 `sparkles` 符号，方便快速识别。

- 新增图标更换功能  
  现在可以为新创建的微信副本更换图标，更方便区分不同用途的多开实例。

- 优化临时文件夹中的同名文件处理  
  当临时文件夹中已存在同名文件时，应用会自动覆盖，而不是直接报错，减少重复操作时的中断。
